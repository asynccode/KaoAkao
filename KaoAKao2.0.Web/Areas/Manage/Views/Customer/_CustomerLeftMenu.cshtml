
@{
    string action = Url.RequestContext.RouteData.Values["action"].ToString();
}
<div class="controller select">
    <span class="ico-img"></span>
    <span class="bolder menu-name">会员管理</span>
    <ul>
        <li class="action "><a class="@(action.ToLower() == "users" ? "select" : "")" href="/Manage/Customer/Users" >会员列表</a></li>
        <li class="action "><a class="@(action.ToLower() == "createteacher" ? "select" : "")" href="/Manage/Customer/CreateTeacher" >新建教师</a></li>
        <li class="action "><a class="@(action.ToLower() == "teachers" ? "select" : "")" href="/Manage/Customer/Teachers" >教师列表</a></li>
        <li class="action "><a class="@(action.ToLower() == "userlevel" ? "select" : "")" href="/Manage/Customer/UserLevel" >会员等级设置</a></li>
    </ul>
</div>
